# Server
PORT=10000
NODE_ENV=development

# CORS (comma-separated). Use '*' only for quick local tests.
CORS_ORIGINS=http://localhost:5173,http://localhost:5500,https://usedashlabs.tech/

# Proof HMAC secret (Render will auto-generate via render.yaml)
PROVER_SECRET=replace-me-in-prod

# Chain config (optional, reserved for future on-chain integration)
SOLANA_CLUSTER=devnet
RPC_URL=
